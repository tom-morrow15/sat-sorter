# Netlify build & deploy configuration
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any path without a matching file
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
